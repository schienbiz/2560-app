name: Keep-alive (app)

on:
  schedule:
    - cron: "0 12 * * *"   # noon UTC — wakes Render in the 10:00-18:00 quiet window
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -sf "${{ secrets.APP_URL }}/health" > /dev/null
